params.input = "data/"
params.outdir = "results/"

process {
  withName:preprocess {
    beforeScript = "chmod +x bin/preprocess.py"
  }
  withName:.* {
    scratch = true
  }
}

env.PATH = "${baseDir}/bin:$PATH"
